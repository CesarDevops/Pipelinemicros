pipeline { 
    agent any 
    tools {
        gradle "Gradle 8.0"
    }
    stages{  
        stage('Build') { 
            steps { 
                sh 'gradle task'
                sh 'gradle init'
                sh 'gradle wrapper'
                sh './gradle build'
                echo 'build succesfull' 
            }
        }
        stage('Test'){
            steps {
                //sh 'make check'
                echo 'test succesfull' 
            }
        }
        stage('Deploy') {
            steps {
                //sh 'make publish'
                echo 'Deploy succesfull'  
            }
        }
    }
}